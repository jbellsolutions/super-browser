{
  "goal": "Draft a LinkedIn comment replying to a post, place it in the comment box, but do not publish until approved.",
  "expected_route": ["rtrvr", "browser-use", "browserbase-stagehand"],
  "approval_required": true
}

