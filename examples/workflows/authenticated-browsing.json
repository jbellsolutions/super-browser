{
  "goal": "Use my logged-in browser session to extract the latest notifications from a private dashboard.",
  "expected_route": ["rtrvr", "browserbase-stagehand", "browser-use"],
  "approval_required": true,
  "required_env": ["RTRVR_API_KEY"]
}

